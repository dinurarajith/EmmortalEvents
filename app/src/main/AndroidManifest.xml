<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emmortalevents">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmmortalEvents">
        <activity android:name=".SelectedPerson"></activity>
        <activity android:name=".AddNewPerson" />
        <activity android:name=".EnrolledPeople" />
        <activity android:name=".SelectedPastEvent" />
        <activity android:name=".PastEvents" />
        <activity android:name=".SelectedEvent" />
        <activity android:name=".CurrentEvents" />
        <activity android:name=".AddEvent" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>